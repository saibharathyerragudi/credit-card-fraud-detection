# Data Directory

Place the Kaggle dataset here before running the notebook or baseline training script:

```text
data/creditcard.csv
```

The raw dataset is not committed because it is large. The `.gitignore` file excludes CSV files in this directory.
